# Coding Workflow as Optimization Function

```markdown
═══════════════════════════════════════════════════════════════
CODING WORKFLOW AS OPTIMIZATION FUNCTION
═══════════════════════════════════════════════════════════════

Domain:
  Π = objective space
  C = codebase states  
  P = plan space
  R = research/context space
  V = validation space (reward signal)

═══════════════════════════════════════════════════════════════
CORE FUNCTIONS
═══════════════════════════════════════════════════════════════

refine:    Π → Π                    (expand ambiguity → clarity)
research:  (Π, C) → R               (compress codebase → relevant truth)
plan:      (Π, R) → P               (synthesize → implementation steps)
review:    (P, Π) → V               (evaluate plan against objective)
implement: (P, C) → C'              (execute plan → new codebase state)

═══════════════════════════════════════════════════════════════
REVIEW AS REWARD FUNCTION
═══════════════════════════════════════════════════════════════

V = (score, δ)

Where:
  score: P → [0,1]                  (quality metric)
  δ:     P → Π                      (gradient: "what's wrong/missing")
  
  score(P) ≥ θ  →  accept           (θ = quality threshold)
  score(P) < θ  →  iterate with δ   (δ guides next refinement)

═══════════════════════════════════════════════════════════════
SINGLE ITERATION
═══════════════════════════════════════════════════════════════

step: (Π, C) → (P, V)

step(π, C) = 
  let π₁ = refine(π)
  let R  = research(π₁, C)
  let P  = plan(π₁, R)
  let V  = review(P, π₁)
  in (P, V)

═══════════════════════════════════════════════════════════════
PLANNING LOOP (fixed C, optimize P)
═══════════════════════════════════════════════════════════════

plan*: (Π, C) → P

plan*(π₀, C) = 
  let (P, (s, δ)) = step(π₀, C)
  in if s ≥ θ 
     then P
     else plan*(π₀ ⊕ δ, C)          (⊕ = incorporate feedback)

Converges when: score(Pₙ) ≥ θ
Invariant: C unchanged during planning

═══════════════════════════════════════════════════════════════
FULL BUILD CYCLE (C evolves)
═══════════════════════════════════════════════════════════════

build: (Π, C) → C'

build(π, C) = 
  let P = plan*(π, C)
  in implement(P, C)

═══════════════════════════════════════════════════════════════
DEVELOPMENT AS TRAJECTORY
═══════════════════════════════════════════════════════════════

Given objective sequence [π₁, π₂, ..., πₙ]:

dev: ([Π], C₀) → C_final

dev([], C) = C
dev(π::πs, C) = dev(πs, build(π, C))

Or as fold:
  C_final = foldl(λC.λπ. build(π, C), C₀, [π₁...πₙ])

═══════════════════════════════════════════════════════════════
THE FULL PICTURE
═══════════════════════════════════════════════════════════════

                    ┌─────────────────────────────────┐
                    │         PLANNING LOOP           │
                    │  (optimize P until score ≥ θ)   │
                    │                                 │
     π₀ ──refine──► π₁ ──┬──► research(π₁,C) = R     │
                    ▲    │                            │
                    │    └──► plan(π₁,R) = P          │
                    │              │                  │
                    │              ▼                  │
                    │         review(P,π₁) = (s,δ)   │
                    │              │                  │
                    │    s < θ?────┴────s ≥ θ?       │
                    │      │              │          │
                    └──────┘              ▼          │
                    (π₁ ⊕ δ)        P (validated)    │
                                         │           │
                    └─────────────────────│───────────┘
                                         │
                                         ▼
                              implement(P, C) = C'
                                         │
                                         ▼
                                   next objective
```

---

**Key insight:** Review isn't just a gate—it's the **loss function gradient**. The δ tells you *how* to improve, not just *whether* to improve.

**What this implies for tooling:**

- Your review step needs to output structured feedback, not just pass/fail
- The ⊕ operator (incorporating δ) is where most productivity gains hide
- θ (quality threshold) is context-dependent—ship fast vs. ship correct

```markdown
═══════════════════════════════════════════════════════════════
THE ⊕ OPERATOR: FEEDBACK INCORPORATION
═══════════════════════════════════════════════════════════════

Feedback structure:

δ = {
  type:    {missing | incorrect | unclear | scope}
  target:  component ∈ {π, R, P}
  signal:  what's wrong (structured critique)
  suggest: potential fix (optional)
}

═══════════════════════════════════════════════════════════════
⊕ : (Π, δ) → Π'
═══════════════════════════════════════════════════════════════

The operator dispatches on feedback type:

(π ⊕ δ) = match δ.type with

  | missing →
      π' = π ∪ {δ.signal}
      -- Example: "auth not considered" → add auth requirement
      
  | incorrect →
      π' = π[δ.target ↦ δ.suggest]  
      -- Example: "wrong API" → substitute correct one
      
  | unclear →
      π' = refine(π, context=δ.signal)
      -- Example: "edge case ambiguous" → force clarification
      
  | scope →
      π' = constrain(π, bounds=δ.signal)
      -- Example: "too broad" → narrow to core requirement

═══════════════════════════════════════════════════════════════
PROPERTIES OF GOOD ⊕
═══════════════════════════════════════════════════════════════

1. Monotonic progress (no regression):
   score(plan(π ⊕ δ, R)) ≥ score(plan(π, R))
   
2. Idempotent on success:
   score(P) ≥ θ  →  (π ⊕ δ) = π
   
3. Convergent (δ shrinks each iteration):
   |δₙ₊₁| ≤ |δₙ|  (feedback diminishes as plan improves)

═══════════════════════════════════════════════════════════════
TERMINATION CONDITIONS
═══════════════════════════════════════════════════════════════

Γ: (P, V, History) → {continue | halt(reason)}

halt conditions:

  ┌─────────────────────────────────────────────────────────────
  │ SUCCESS
  ├─────────────────────────────────────────────────────────────
  │ score(P) ≥ θ                         
  │ → halt(accepted)
  └─────────────────────────────────────────────────────────────

  ┌─────────────────────────────────────────────────────────────
  │ RESOURCE EXHAUSTION  
  ├─────────────────────────────────────────────────────────────
  │ n ≥ max_iterations                   
  │ → halt(budget_exceeded)
  │
  │ elapsed ≥ deadline                   
  │ → halt(timeout)
  └─────────────────────────────────────────────────────────────

  ┌─────────────────────────────────────────────────────────────
  │ CONVERGENCE FAILURE
  ├─────────────────────────────────────────────────────────────
  │ |scoreₙ - scoreₙ₋₁| < ε  ∧  score < θ    
  │ → halt(plateau)
  │ -- Improving but not enough to matter
  │
  │ scoreₙ < scoreₙ₋₁                    
  │ → halt(regression)  
  │ -- Getting worse (⊕ failed its contract)
  │
  │ δₙ = δₙ₋₁                            
  │ → halt(stuck)
  │ -- Same feedback twice = can't incorporate it
  └─────────────────────────────────────────────────────────────

  ┌─────────────────────────────────────────────────────────────
  │ OBJECTIVE INFEASIBILITY
  ├─────────────────────────────────────────────────────────────
  │ δ.type = impossible                  
  │ → halt(infeasible)
  │ -- Review determined objective can't be met with current C
  │
  │ contradicts(π₁, C)                   
  │ → halt(conflict)
  │ -- Objective incompatible with codebase constraints
  └─────────────────────────────────────────────────────────────

═══════════════════════════════════════════════════════════════
REFINED PLANNING LOOP WITH TERMINATION
═══════════════════════════════════════════════════════════════

plan*: (Π, C, Config) → Result

Result = Ok(P) | Err(reason)

Config = {
  θ:              [0,1]        -- quality threshold
  max_iter:       ℕ            -- iteration budget  
  ε:              [0,1]        -- minimum progress threshold
  deadline:       Time         -- wall clock limit
}

plan*(π₀, C, cfg) = loop(π₀, [], 0)
  where
    loop(π, history, n) =
      let (P, (s, δ)) = step(π, C)
      let history'    = history ++ [(π, P, s, δ)]
      in match Γ(P, (s, δ), history', cfg) with
        | halt(accepted)  → Ok(P)
        | halt(reason)    → Err(reason, history', best(history'))
        | continue        → loop(π ⊕ δ, history', n+1)

    best(history) = argmax(λ(_, P, s, _). s, history)
    -- Return best attempt even on failure

═══════════════════════════════════════════════════════════════
DECISION TREE ON TERMINATION
═══════════════════════════════════════════════════════════════

Result → Action

Ok(P)                    → implement(P, C)

Err(budget_exceeded, h)  → human_decision(best(h))
                           -- "Good enough?" or allocate more budget

Err(plateau, h)          → decompose(π) 
                           -- Break into smaller objectives

Err(stuck, h)            → escalate(δ)
                           -- Need human to resolve feedback

Err(infeasible, h)       → revise_objective(π)
                           -- Objective impossible, must change it

Err(regression, h)       → rollback(h) ∧ fix(⊕)
                           -- ⊕ operator is broken

═══════════════════════════════════════════════════════════════
VISUAL: FULL STATE MACHINE
═══════════════════════════════════════════════════════════════

                    ┌──────────────┐
                    │     π₀       │
                    └──────┬───────┘
                           │
                           ▼
               ┌───────────────────────┐
               │     step(π, C)        │
               │  → (P, score, δ)      │
               └───────────┬───────────┘
                           │
                           ▼
               ┌───────────────────────┐
               │      Γ(P, V, H)       │◄────────────┐
               │   termination check   │             │
               └───────────┬───────────┘             │
                           │                         │
          ┌────────────────┼────────────────┐        │
          │                │                │        │
          ▼                ▼                ▼        │
    ┌──────────┐    ┌────────────┐   ┌──────────┐   │
    │ accepted │    │  continue  │   │  error   │   │
    └────┬─────┘    └──────┬─────┘   └────┬─────┘   │
         │                 │              │         │
         ▼                 │              ▼         │
    ┌──────────┐           │      ┌─────────────┐   │
    │implement │           │      │   handle    │   │
    │ (P, C)   │           │      │   error     │   │
    └────┬─────┘           │      └─────────────┘   │
         │                 │              │         │
         ▼                 │              ▼         │
    ┌──────────┐           │      {decompose,      │
    │    C'    │           │       escalate,       │
    └──────────┘           │       revise,         │
                           │       rollback}       │
                           │                       │
                           └───────► π ⊕ δ ────────┘

═══════════════════════════════════════════════════════════════
KEY INSIGHT
═══════════════════════════════════════════════════════════════

The ⊕ operator is where your competitive advantage lives.

Generic:     π ⊕ δ = "try again with feedback"  (slow, human-dependent)
Structured:  π ⊕ δ = dispatch(δ.type)           (fast, automatable)

The better your δ structure, the more mechanical ⊕ becomes.
The more mechanical ⊕ becomes, the more you can automate.

This is your consulting→automation bridge:
1. Consulting: discover δ patterns manually
2. Structure: categorize into {missing, incorrect, unclear, scope}
3. Automate: build ⊕ handlers for each category
```

```markdown
═══════════════════════════════════════════════════════════════
FEEDBACK UPDATES PLAN
═══════════════════════════════════════════════════════════════

Setup (computed once):
  π = refine(π₀)
  R = research(π, C)

Loop (over plan only):
  v(P) = review(P, π) → (score, δ)

───────────────────────────────────────────────────────────────
RECURSIVE
───────────────────────────────────────────────────────────────

W(π₀, C) = 
  let π = refine(π₀)
      R = research(π, C)
      P*(P) = v(P).s ≥ θ  ?  P  :  P*(P ⊕ v(P).δ)
  in P*(plan(π, R))


───────────────────────────────────────────────────────────────
FIXED-POINT (cleanest)
───────────────────────────────────────────────────────────────

W(π₀, C) = μP.(v(P).s ≥ θ → P | P ⊕ v(P).δ)
           where P₀ = plan(refine(π₀), research(refine(π₀), C))


───────────────────────────────────────────────────────────────
ONE LINE
───────────────────────────────────────────────────────────────

W(π₀, C) = fix(λP. P ⊕ δ(P)·𝟙[s(P)<θ], plan(π, R))


───────────────────────────────────────────────────────────────
WHITEBOARD VERSION
───────────────────────────────────────────────────────────────

        ┌─────────────────────────────────┐
        │  π = refine(π₀)                 │  once
        │  R = research(π, C)             │  once
        │  P₀ = plan(π, R)                │  once
        └───────────────┬─────────────────┘
                        ▼
              ┌─────────────────┐
              │  Pₙ₊₁ = Pₙ ⊕ δₙ │ ◄───┐    until s(P) ≥ θ
              └────────┬────────┘     │
                       │              │
                       ▼              │
                   review(Pₙ)─────────┘
```
